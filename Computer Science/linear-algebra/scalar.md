# Scalar

A scalar is a quantity that has only magnitude/size, so something like 10kg, 500 meters, 200 degrees Celsius, etc.
Also, it belongs to the real numbers field (i.e. any single number a ∈ ℝ.)
