# Vector

Vectors are quantities that have both magnitude/size and direction. We write it as v.

And like a scalar, any vector belongs to the real numbers field (i.e. any single number a ∈ ℝ.).
Or in a more formal definition, v ∈ ℝⁿ (n×1).
